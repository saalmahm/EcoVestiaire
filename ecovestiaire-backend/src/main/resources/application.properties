spring.application.name=EcoVestiaire

# Profil actif
spring.profiles.active=dev
<<<<<<< HEAD
spring.config.import=optional:file:.env[.properties]
=======
spring.config.import=file:.env[.properties]
>>>>>>> 23d40d5 (Fix: Restructuration complète du projet, correction des erreurs de compilation Lombok et résolution du problème 401 Unauthorized)

# =========================
#  DATABASE (profil dev)
# =========================
spring.datasource.url=jdbc:postgresql://localhost:5432/ecovestiaire_db
spring.datasource.username=postgres
spring.datasource.password=salmahm
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA / Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.format_sql=true

# stripe.secret-key=${STRIPE_SECRET_KEY}
# stripe.publishable-key=${STRIPE_PUBLISHABLE_KEY}
# stripe.webhook-secret=${STRIPE_WEBHOOK_SECRET}

# Port HTTP
server.port=8080